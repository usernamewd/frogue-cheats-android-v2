# Project-wide Gradle settings
org.gradle.jvmargs=-Xmx2g -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true
android.enableJetifier=true

# Build optimizations
android.enableR8.fullMode=true
android.enableBuildCache=true

# CI/CD specific settings
org.gradle.daemon=false
org.gradle.configureondemand=true

# Android build settings
android.buildType=release
android.keystorePassword=android
android.keyPassword=android
android.keyAlias=androiddebugkey
android.keystoreFile=debug.keystore

# Memory settings for faster builds
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError

# Enable AndroidX
android.useAndroidX=true
android.enableJetifier=true